{
  "name": "@beon-tech/ai-score",
  "version": "0.1.10",
  "description": "Extracts AI coding harness usage (Claude Code, Codex, Cursor, OpenCode, pi) into an auditable, privacy-preserving report and submits it to Beon for scoring.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/BEON-Tech/ai-score.git"
  },
  "bin": {
    "ai-score": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "WIRE_FORMAT.md"
  ],
  "type": "module",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "node --test \"test/*.test.js\"",
    "prepublishOnly": "pnpm build",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "oxfmt": "^0.60.0",
    "typescript": "^7.0.2"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@11.17.0"
}
